import CampBooking from './components/SquareBooking'

export default function App() {
  return (
    <div>
      {/*
        ─────────────────────────────────────────────
        CAMP TINY TAILS — App Entry Point
        ─────────────────────────────────────────────
        Your Hostinger AI-generated site content
        will live here. The booking component is
        already imported above — just drop it
        wherever you want it on the page:

          <CampBooking />

        ─────────────────────────────────────────────
      */}

      {/* Example: booking section */}
      <CampBooking />
    </div>
  )
}
